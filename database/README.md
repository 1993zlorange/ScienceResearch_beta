# Database Boundary

本目录拥有数据库 schema、迁移、种子和维护脚本。当前模板没有产品数据表。

- 在 `schema/` 记录经过批准的结构和关系。
- 在 `migrations/` 追加有序迁移，禁止修改已经进入共享环境的文件。
- 在 `seeds/` 只放本地/测试的非敏感可重复数据。
- 在 `scripts/` 放迁移状态、备份、恢复和验证命令。
- 每次迁移必须说明兼容窗口、锁风险、回填、验证和恢复策略。

数据库选型、连接方式和生产备份策略必须先通过 ADR 确认。
